# By Reference & By Value

Recall that whenever you assign one variable to another, primitive values will essentially be copied within the memory. The variable is actually pass *by value*.

However, when you do this with an object, the two variables are simply *referencing* the same object in memory.
